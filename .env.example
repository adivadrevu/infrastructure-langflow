# Langflow Components Path
# Set to the absolute path of the components directory
LANGFLOW_COMPONENTS_PATH=/path/to/infrastructure-composer-langflow/components

# ============================================================================
# PRIVACY SETTINGS - Tracking/Telemetry Disabled
# ============================================================================
# These settings prevent Langflow from sending any data
# DO NOT change these unless you explicitly want to enable tracking
DO_NOT_TRACK=true
LANGFLOW_DO_NOT_TRACK=true
LANGFLOW_TELEMETRY=false

# Optional: AWS Credentials (can also be provided via UI)
# Uncomment and fill in if you want to use environment variables instead of UI input
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key
# AWS_REGION=us-east-1
# AWS_SESSION_TOKEN=your_session_token

# Langflow Configuration (Optional)
# LANGFLOW_HOST=0.0.0.0
# LANGFLOW_PORT=7860
# LANGFLOW_LOG_LEVEL=INFO
