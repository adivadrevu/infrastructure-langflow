# Minimal Dependencies (for testing components only, without full Langflow UI)
# 
# This is for development/testing only. To run the full Langflow UI, use requirements.txt

# Core Langflow components (minimal)
lfx>=0.2.0
langflow-base>=0.7.0

# AWS SDK
boto3>=1.34.0

# Data Validation
pydantic>=2.0.0

# Note: This won't include the Langflow UI server, only the component framework
